# Database Configuration (cPanel Production)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=bizmanager_main

# JWT Secret (CHANGE THIS - Generate using: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\")
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Origin (Update with your actual domain)
CORS_ORIGIN=http://localhost:3000

# Super Admin JWT Secret (CHANGE THIS - Different from regular JWT)
SUPER_ADMIN_JWT_SECRET=your-super-admin-jwt-secret-change-this-too-987654321
